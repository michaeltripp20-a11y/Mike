[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.build]
cmds = ["rm -f package-lock.json", "npm install", "npm run build", "npm install -g serve"]

[start]
cmd = "sh -c 'serve -s dist -l ${PORT:-4173}'"
