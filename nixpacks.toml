[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.build]
cmds = ["rm -f package-lock.json", "npm install", "npm run build"]

[start]
cmd = "sh -c 'npx vite preview --host 0.0.0.0 --port ${PORT:-4173}'"
