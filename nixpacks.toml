[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["rm -f package-lock.json", "npm install", "npm install -g serve"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "sh -c 'serve -s dist -l ${PORT:-4173}'"
