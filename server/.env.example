SMTP_HOST=smtp.resend.com
SMTP_PORT=587
SMTP_USER=resend
SMTP_PASS=re_your_api_key_here

# Optional: override the sender address (required if using Resend free tier)
# SMTP_FROM=onboarding@resend.dev
# SMTP_FROM=noreply@yourdomain.com
